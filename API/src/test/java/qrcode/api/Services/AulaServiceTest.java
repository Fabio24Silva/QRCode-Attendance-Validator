package qrcode.api.Services;

import org.junit.Test;
import qrcode.api.Models.CacheObject;

import java.time.LocalTime;
import java.util.UUID;

import static org.junit.Assert.*;

/**
 *
 * Title: QRCode Attendance API
 *
 * @author Fábio Silva
 * @version 1.0
 */
public class AulaServiceTest {
    @Test
    public void newAula() {
//        AulaService aulaService = new AulaService();
//        assertNotNull(aulaService.newAula(2, LocalTime.now(), LocalTime.now()));
//        CacheObjectService cacheObjectService = new CacheObjectService();
//        //  long uuid=UUID.randomUUID().getMostSignificantBits();
//        long uuid = 3020121;
//        //  CacheObject cacheObject = new CacheObject(uuid,LocalTime.now() );
//
//        //cacheObject.renew(1L, LocalTime.of(10,0,5));
//        long id = (long) 2;
//        aulaService.newAula(2, LocalTime.now(), LocalTime.now());
//        //so da true se o uuid for aquele definido em cima
//       assertTrue(cacheObjectService.checkUuID(id, uuid, LocalTime.now(), "dsad", 35522));
    }
}